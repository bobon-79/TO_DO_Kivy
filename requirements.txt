# Основные зависимости
kivy>=2.3.0

# Если будем использовать SQLite - встроен в Python, отдельной зависимости нет
# Если захотим JSON - встроен в Python, отдельной зависимости нет

# Для разработки (опционально)
# buildozer>=1.5.0   # для Android-сборки
# pyinstaller>=6.0.0 # для сборки exe на десктоп
